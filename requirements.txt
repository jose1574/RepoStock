# Dependencias opcionales. Python ya incluye sqlite3, pero si prefieres usar
# una implementación empaquetada, descomenta la línea siguiente e instala.
# pysqlite3-binary>=0.4.0

# Core de la aplicación web
Flask==3.0.0
Werkzeug==3.0.0

# Autenticación y formularios
Flask-Login==0.6.2
Flask-WTF==1.1.1
WTForms==3.0.1
email-validator==2.0.0

# ORM (si se usa en el proyecto)
SQLAlchemy==2.0.0

# Carga de variables de entorno desde .env
python-dotenv==1.0.0

# Generación de PDFs (requiere instalar wkhtmltopdf en el sistema)
pdfkit==1.0.0

# PostgreSQL driver (binario recomendado en Windows)
psycopg2-binary==2.9.9

# Servidor WSGI de producción
waitress==2.1.2

# Soporte para servicios de Windows (pywin32)
pywin32==306

